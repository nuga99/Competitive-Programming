#include<bits/stdc++.h>
/** MACROS **/
#define REP(i,a,b) for (int i = int(a); i <= int(b); i++)  // a ke b, dengan i variabel local
#define TRvi(c,it) for (vi::iterator it = (c).begin(); it!=(c).end();it++);

#define pb push_back

#define INF 2000000000 // 2 milliar
#define MEMSET_INF 127 // sekitar 2 miliar
#define MEMSET_HALF_INF 63 //sekitar 1 miliar
#define MOD 10000009 // 1e9

typedef long long ll;

using namespace std;

struct cc{
};

int main(){


return 0;
}
